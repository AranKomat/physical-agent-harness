# Active Model Selection Update

Operator decision: retire Luna from subsequent experiments. The active BEHAVIOR
configuration now accepts only Sol and Astra; Sol remains the default. GLM
remains the current inventory candidate. Historical Luna responses, billing
receipts and analysis are preserved, not rewritten or deleted. The private
Sol/Luna batch entry point refuses further dispatch before loading credentials.

Plan a matched Sol-versus-Astra executive/verifier comparison after the motor
gates support meaningful executed decisions. Use the same observations, prompt,
context/memory tier, reasoning effort, frozen motor, task starts, action/call
budgets and independent verification. Balance condition order and retain failed
attempts. Report task progress/success, recoveries, calls, cost and latency.
Public robot demos motivate the hypothesis that Astra may help; they are not
evidence of a local performance gap. No winner is preselected.

If subscription-backed Codex is later considered for Astra development use,
evaluate it as a separate transport cohort: its runtime context is not identical
to the API. Do not mix its results into the controlled API model comparison.
Neither that transport experiment nor new paid calls are authorized here.
Existing campaign ceilings and unresolved holds remain unchanged.

Verification: 1,489 public tests, Ruff, five fixture CLIs and repository ownership
checks pass. Private suite: 969 passed, one existing skip; focused private Ruff
passes. No model calls were made for this change.
